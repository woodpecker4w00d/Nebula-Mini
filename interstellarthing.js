javascript: (function () {    var overlay = document.createElement("div");    overlay.style.position = "fixed";    overlay.style.top = "50%";    overlay.style.left = "50%";    overlay.style.transform = "translate(-50%,-50%)";    overlay.style.width = "80%";    overlay.style.height = "80%";    overlay.style.backgroundColor = "rgba(0,0,0,0.8)";    overlay.style.boxShadow = "0 0 10px rgba(0,0,0,0.5)";    overlay.style.zIndex = "9999";    var navbar = document.createElement("div");    navbar.style.height = "40px";    navbar.style.backgroundColor = "#003366";    navbar.style.color = "#fff";    navbar.style.display = "flex";    navbar.style.alignItems = "center";    navbar.style.padding = "0 5px";    navbar.innerText = "Code Terminal";    var closeButton = document.createElement("button");    closeButton.innerText = "X";    closeButton.style.marginLeft = "auto";    closeButton.style.cursor = "pointer";    closeButton.style.backgroundColor = "transparent";    closeButton.style.color = "#fff";    closeButton.style.border = "none";    closeButton.style.fontSize = "10px";    closeButton.onclick = function () {        document.body.removeChild(overlay);    };    navbar.appendChild(closeButton);    overlay.appendChild(navbar);    var iframe = document.createElement("iframe");    iframe.style.width = "100%";    iframe.style.height = "calc(100% - 40px)";    iframe.style.border = "none";    iframe.src = "https://gointerstellar.app";    overlay.appendChild(iframe);    document.body.appendChild(overlay);    var isDragging = false,        offsetX,        offsetY;    navbar.addEventListener("mousedown", function (e) {        isDragging = true;        offsetX = e.clientX - overlay.getBoundingClientRect().left;        offsetY = e.clientY - overlay.getBoundingClientRect().top;    });    document.addEventListener("mousemove", function (e) {        if (isDragging) {            overlay.style.left = e.clientX - offsetX + "px";            overlay.style.top = e.clientY - offsetY + "px";            overlay.style.transform = "none";        }    });    document.addEventListener("mouseup", function () {        isDragging = false;    });})();
